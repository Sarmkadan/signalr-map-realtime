# PagedRequestExtensions

`PagedRequestExtensions` provides a set of static utility methods for handling pagination, filtering, and sorting operations on `PagedRequest` objects. These extensions simplify the calculation of pagination parameters, validation of input values, and application of common query modifications such as search queries and sorting criteria.

## API

### `CalculateSkip`
