# ApplicationDbContext

`ApplicationDbContext` is the primary Entity Framework Core database context for the `signalr-map-realtime` project. It manages entity sets for user accounts, vehicle tracking, location data, routes, and related assets, enabling real-time mapping and tracking functionality via SignalR.

## API

### Constructor
